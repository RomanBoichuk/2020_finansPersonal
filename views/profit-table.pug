extends loyout
block content
  h2 Додати доходи
  form.profit-table(action='/profit-table' method="POST")
    p
      input(name="cost" placeholder= "введіть дохід" value = "")
    p
      input(name="price" placeholder="сума доходу" value = "")
    button ЗБЕРЕГТИ
